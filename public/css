:root { --accent-text:#f0e6c8; }

body {
  font-family: 'Press Start 2P', sans-serif;
  background: #0b3b20 url('../imagens/floresta1.png') center/cover no-repeat;
  color: var(--accent-text);
}

.scene {
  display:grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap:40px;
  align-items:start;
  justify-items:center;
}

.character {
  display:flex;
  flex-direction:column;
  align-items:center;
}

.character img {
  width:200px;
  filter: drop-shadow(0 2px 10px rgba(0,0,0,0.6));
}

.speech {
  margin-top:12px;
  background:#f0e6c8;
  color:#0a2b17;
  padding:12px 16px;
  border-radius:20px;
  border:4px solid #b59f6b;
  font-size:12px;
  max-width:220px;
  text-align:center;
  box-shadow:0 6px 14px rgba(0,0,0,0.45);
}
.phase-card {
  background: rgba(10, 43, 23, 0.45);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius:20px;
  padding:20px;
  width: 320px;
  color: var(--accent-text);
  text-align:center;
  backdrop-filter: blur(12px);
}
.question button {
  width:100%;
  margin:5px 0;
  padding:10px;
  border-radius:10px;
  border:none;
  background:#2a7b3e;
  color:#f7f0d6;
  cursor:pointer;
  font-size:12px;
  transition:0.3s;
}
.question button:hover { opacity:0.85; transform:translateY(-2px);}
.next-btn {
  margin-top:10px;
  padding:10px 20px;
  border:none;
  border-radius:10px;
  cursor:pointer;
  background:#1e5f2d;
  color:#f7f0d6;
}
